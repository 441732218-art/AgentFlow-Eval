import { Typography } from "antd";

export default function Settings() {
  return (
    <div>
      <Typography.Title level={4}>Settings</Typography.Title>
      <Typography.Text type="secondary">Settings page - under development.</Typography.Text>
    </div>
  );
}

# AgentFlow Intelligence v1.0.0

**Release date:** 2026-07-21  
**Product:** AgentFlow Intelligence ([AgentFlow-Eval](https://github.com/441732218-art/AgentFlow-Eval))  
**Positioning:** Enterprise Agent evaluation platform — private deploy / Lite demo / SaaS skeleton  

---

## 中文说明

### 主要功能

- **评测主链路**：任务 → 用例 → 异步/同步执行 → Trace → LLM-as-Judge / 规则评分 → 报告  
- **Intelligence Center**：Dashboard 驾驶舱、Trace Explorer、故障诊断、Analytics、实时监控  
- **企业能力**：多租户（请求头 `X-Tenant-ID`）、企业 RBAC、审计日志、API Key 鉴权  
- **商业化骨架**：套餐 / 订阅 / 配额（HTTP 429）、Billing API、Stripe Checkout（mock）  
- **Benchmark**：基准套件与跑分引擎（API + UI）  
- **扩展**：插件系统（allowlist / 可选签名）、A/B 测试、多模态媒体评估  
- **可观测**：Prometheus `/metrics`、AOLS 结构化日志、慢任务与 KPI  
- **部署剖面**：`lite`（零 Redis Worker）/ `private`（Docker Compose）/ `saas`  
- **交付形态**：Web/PWA、Electron 桌面（Windows / macOS）、Docker 镜像、离线交付包脚本  

### 下载与安装

#### 1) Desktop（推荐本地演示）

本 Release **Assets** 中提供：

| 文件 | 平台 | 说明 |
| --- | --- | --- |
| `AgentFlow-1.0.0-win-x64.exe` | Windows x64 | NSIS 安装包 |
| `AgentFlow-1.0.0-win-portable.exe` | Windows x64 | 便携版，免安装 |
| `AgentFlow-1.0.0-win-x64.zip` | Windows x64 | 解压即用 |
| `AgentFlow-1.0.0-mac-arm64.dmg` | macOS Apple Silicon | 磁盘映像安装 |
| `AgentFlow-1.0.0-mac-arm64.zip` | macOS arm64 | 压缩包 |

> **说明**  
> - 当前桌面包为 **UI shell**（内嵌前端静态资源）。完整评测 API 需另起后端（Eager Lite 或 Docker private 栈）。  
> - Windows 构建为 **未代码签名** 内部发行版，SmartScreen 可能提示，选择「仍要运行」即可。  

#### 2) 容器镜像（GHCR）

```bash
# 登录（公开拉取可按仓库可见性跳过）
echo $GHCR_TOKEN | docker login ghcr.io -u USERNAME --password-stdin

docker pull ghcr.io/441732218-art/agentflow-eval-backend:1.0.0
docker pull ghcr.io/441732218-art/agentflow-eval-frontend:1.0.0
```

私有化一键：

```bash
export BACKEND_IMAGE=ghcr.io/441732218-art/agentflow-eval-backend:1.0.0
export FRONTEND_IMAGE=ghcr.io/441732218-art/agentflow-eval-frontend:1.0.0
cd backend
# 准备 .env.docker 后：
docker compose -f docker-compose.prod.yml --env-file .env.docker up -d
```

详见仓库根目录 [DEPLOYMENT_GUIDE.md](https://github.com/441732218-art/AgentFlow-Eval/blob/main/DEPLOYMENT_GUIDE.md)。

#### 3) 源码 / Lite 开发路径

```powershell
# Windows Eager Lite（无需 Redis Worker）
powershell -ExecutionPolicy Bypass -File scripts\start-lite.ps1
```

### 已知限制

1. **Stripe 为 mock 路径**：生产收款需自行配置真实 Stripe 密钥与 Webhook。  
2. **桌面端不内嵌后端服务**：需配合 API 地址 / Lite 或 Docker。  
3. **Windows 安装包未 Authenticode 签名**。  
4. **插件动态加载**依赖 allowlist；签名校验为可选开关，默认严格模式视部署配置。  
5. **覆盖率门禁约 40%**：持续提升中，非 80% 企业审计目标。  
6. **macOS 产物当前以 arm64 为主**（Apple Silicon CI）。  
7. **离线 Docker tar** 需本地执行 `scripts/export-offline-package.ps1` 生成，不随 git 发布。  

### 后续计划（v1.1+）

- [ ] 桌面端可选内嵌 / 一键唤起本地 API  
- [ ] Bandit / 依赖审计与镜像扫描持续硬化  
- [ ] 覆盖率与 E2E 门禁提升  
- [ ] Linux Desktop / 更多架构安装包  
- [ ] 真实 Stripe 与计费对账闭环  
- [ ] 插件市场签名与沙箱隔离增强  

### 安全与合规

- 请勿将含真实密钥的 `.env` / `.env.docker` 提交或打包进交付物。  
- 生产请设置强 `SECRET_KEY`、`API_KEYS`、数据库密码，并开启 `AUTH_ENABLED=true`。  
- 安全基线见 [SECURITY.md](https://github.com/441732218-art/AgentFlow-Eval/blob/main/SECURITY.md)、[docs/security-audit-report.md](https://github.com/441732218-art/AgentFlow-Eval/blob/main/docs/security-audit-report.md)。  

---

## English Summary

**AgentFlow Intelligence v1.0.0** is an enterprise-oriented **Agent evaluation platform** with a full task → trace → judge → report pipeline, Intelligence Center UI, multi-tenant RBAC, billing skeleton, benchmarks, plugins, and deploy profiles (`lite` / `private` / `saas`).

### Artifacts

- **Desktop installers** (Windows NSIS / portable, macOS arm64) on this Release  
- **Container images:** `ghcr.io/441732218-art/agentflow-eval-backend:1.0.0` and `...-frontend:1.0.0`  
- **Source + Compose** private deploy — see `DEPLOYMENT_GUIDE.md`  

### Known limits

Mock Stripe, desktop UI shell (API runs separately), unsigned Windows builds, coverage gate ~40%.

### License

MIT — see repository `LICENSE`.
